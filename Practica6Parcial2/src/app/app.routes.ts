import { Routes } from '@angular/router';
import { PronosticoComponent } from './pages/pronostico/pronostico.component';

export const routes: Routes = [
    {
        //localhost:4200/pronostico
        path: 'pronostico', component: PronosticoComponent
    }
];
